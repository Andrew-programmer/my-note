import React from 'react';

const style = {
    logo: `text-xl md:text-3xl text-white mx-1`
}

const Logo = () => {
    return (
        <h1 className={style.logo}>
            My Note
        </h1>
    );
};

export default Logo;
